import ReadingLibrary from '@/components/dashboard/reading/reading-library';

export const metadata = { title: 'Reading | Lingua', description: 'Luyện đọc TOEIC theo Part và độ khó.' };

export default function ReadingPage() {
  return <ReadingLibrary />;
}
